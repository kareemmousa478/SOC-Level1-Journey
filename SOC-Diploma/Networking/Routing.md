# Routing Configuration

## Static Routing

Static routing was configured on one side of the enterprise topology.

Static routes were used to provide paths toward remote networks.

## OSPF

OSPF was configured on the other side of the topology using:

- OSPF Process ID
- Area 0
- Network advertisements

## Routing Verification

Connectivity was verified using commands such as:

```text
show ip route
show ip ospf neighbor
show ip protocols
ping
traceroute
